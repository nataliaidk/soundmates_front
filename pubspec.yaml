name: zpi_test
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  jwt_decoder: ^2.0.1
  dropdown_search: ^5.0.6
  emoji_picker_flutter: ^2.0.0
  flutter_map: ^7.0.1
  latlong2: ^0.9.1
  signalr_netcore: ^1.3.1
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  flutter_dotenv: ^5.1.1
  flutter_secure_storage: ^8.0.0
  http_parser: ^4.0.0
  file_picker: ^8.1.4
  provider: ^6.0.5
  uuid: ^3.0.0
  url_launcher: ^6.2.0
  
  # Media playback packages for visit_profile
  just_audio: ^0.9.36
  photo_view: ^0.14.0
  video_player: ^2.8.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - lib/assets/logo.png

  #fix